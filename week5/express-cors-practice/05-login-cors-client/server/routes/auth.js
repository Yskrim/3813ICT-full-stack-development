// TODO: class User { constructor(username, birthdate, age, email, password) { ... } }

// TODO: array of 3 users using the User class above

module.exports = function (app) {
	// TODO: POST /api/auth
	// body: { username, password }
	// - match found  -> 200, user object with valid: true, WITHOUT password
	// - no match     -> 200, { valid: false }
};
