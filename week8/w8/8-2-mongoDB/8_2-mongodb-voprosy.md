# 8_2 — MongoDB: вопросы для самопроверки

**Курс:** 3813ICT, неделя 8
**Связанные файлы:** [конспект](8_2-mongodb-konspekt.md) · [поправки](8_2-mongodb-popravki.md) · [примеры](8_2-mongodb-primery.md) · [ответы](8_2-mongodb-otvety.md)

**Как работать:**

1. Отвечай по памяти, не открывая конспект. Для команд сначала предскажи результат, потом проверь в `mongosh`.
2. Если не знаешь — так и отметь «не знаю»: это честнее и полезнее догадки.
3. После каждого раздела сверяйся с ответами (ссылка «→ ответ» под вопросом).
4. Вопросы, на которых ошибся, повтори через день.

---

## A. Инструменты и установка

<a id="q-1"></a>

### 1. Кто есть кто

Чем отличаются `mongod`, `mongosh` и npm-пакет `mongodb`? Кто из них хранит данные?

[→ ответ](8_2-mongodb-otvety.md#a-1)

<a id="q-2"></a>

### 2. Установка из материала

Что не так с этой установкой на современном macOS и как установить и запустить MongoDB сейчас?

```
brew install mongodb
sudo chown -R `id –un` /data/db
mongod
```

[→ ответ](8_2-mongodb-otvety.md#a-2)

---

## B. Базы, коллекции, документы

<a id="q-3"></a>

### 3. Есть ли база?

```js
use shop
show dbs
```

Будет ли `shop` в списке? Когда база появится на диске?

[→ ответ](8_2-mongodb-otvety.md#a-3)

<a id="q-4"></a>

### 4. Что показал скриншот?

```
> use test
switched to db test
> db.dropDatabse
test.dropDatabse
```

Удалена ли база `test`? Что на самом деле произошло и как удалить базу правильно и безопасно?

[→ ответ](8_2-mongodb-otvety.md#a-4)

<a id="q-5"></a>

### 5. `id` и `_id`

Чем поле `id` из примеров материала отличается от `_id`? Что произойдёт при второй вставке каждой пары?

```js
db.books.insertOne({ _id: 1, title: 'A' }); db.books.insertOne({ _id: 1, title: 'B' })
db.books.insertOne({ id: 7, title: 'C' });  db.books.insertOne({ id: 7, title: 'D' })
```

[→ ответ](8_2-mongodb-otvety.md#a-5)

---

## C. Чтение

<a id="q-6"></a>

### 6. Проекция

Документы содержат поля `_id`, `test`, `id`. Что вернёт запрос и почему?

```js
db.collectionName.find({}, { id: 1, data: 1 })
```

[→ ответ](8_2-mongodb-otvety.md#a-6)

<a id="q-7"></a>

### 7. Найди ошибки

```js
db.books.find({}, { title: 1, stock: 0 })
db.books.find().count
db.books.find().sort(price: -1)
```

[→ ответ](8_2-mongodb-otvety.md#a-7)

<a id="q-8"></a>

### 8. Число или строка

В одном документе `price: 45`, в другом `price: '45'`. Какие документы найдёт `db.items.find({ price: { $gt: 40 } })`? Почему?

[→ ответ](8_2-mongodb-otvety.md#a-8)

<a id="q-9"></a>

### 9. Исправь шпаргалку

```
AND   find({$or:[<expr1>,<expr2>]})
OR    find({$and:[<expr1>,<expr2>]})
NOT   find({$not:{<expr1>}})
```

Запиши правильно и приведи пример «цена не больше 50» через `$not`.

[→ ответ](8_2-mongodb-otvety.md#a-9)

<a id="q-10"></a>

### 10. Самая дорогая и пагинация

Как найти самую дорогую книгу? Почему `find().limit(1)` без `sort` ненадёжен? Как получить третью страницу по 10 книг, если страницы нумеруются с 1, и почему для длинной ленты сообщений лучше другой способ?

[→ ответ](8_2-mongodb-otvety.md#a-10)

---

## D. Изменение и связи

<a id="q-11"></a>

### 11. Устаревшие методы

Чем заменить `update(query, {$set: ...})`, `remove(query)` и `remove(query, {justOne: true})`? Почему новые методы безопаснее?

[→ ответ](8_2-mongodb-otvety.md#a-11)

<a id="q-12"></a>

### 12. `findOneAndUpdate`

У книги `price: 45`. Что вернёт команда и как получить документ с новой ценой?

```js
db.books.findOneAndUpdate({ _id: 4 }, { $set: { price: 50 } })
```

[→ ответ](8_2-mongodb-otvety.md#a-12)

<a id="q-13"></a>

### 13. Продажа без минуса

Напиши одну команду, которая уменьшает `stock` книги `_id: 1` на 1, только если книга есть на складе. Как по ответу понять, что книги не было?

[→ ответ](8_2-mongodb-otvety.md#a-13)

<a id="q-14"></a>

### 14. Вложенные документы и соединения

Почему `db.users.find({ profile: { city: 'Brisbane' } })` может не найти пользователя с `profile: { age: 25, city: 'Brisbane' }`? Как правильно? И верно ли, что для соединения коллекций нужна рекурсивная функция в Node.js или SQL-база?

[→ ответ](8_2-mongodb-otvety.md#a-14)
