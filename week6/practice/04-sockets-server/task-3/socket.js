// Задание 3 — вся логика событий живёт здесь
// Модуль экспортирует функцию, принимающую io. Сервер он не создаёт.

module.exports = (io) => {
    // TODO: io.on('connection', (socket) => { ... })
    //   - лог подключения
    //   - socket.on('message', ...) → рассылка 'new-message' тем способом,
    //     который ты выбрал в задании 2
    //   - лог отключения
};
